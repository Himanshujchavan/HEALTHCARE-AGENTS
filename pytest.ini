[pytest]
# Only run the main project test suite under /tests.
# The nested Agents/health_agent subproject contains experimental scripts/tests
# that may require external API keys or extra optional dependencies.
testpaths = tests
python_files = test_*.py
